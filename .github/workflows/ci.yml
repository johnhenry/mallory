name: CI

on:
  push:
    branches: [main, master]
  pull_request:

# Supersede an in-flight run when new commits land on the same ref -- the
# whole family shares this block so a rapid push sequence never queues N
# redundant runs.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version: [22.x, 24.x]
    name: node ${{ matrix.node-version }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - run: npm ci

      - name: Set up Python (SciPy/SymPy verification oracle)
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install oracle dependencies (scipy, sympy)
        run: pip install scipy sympy

      - name: Unit tests
        run: npm test

      - name: Build (includes typecheck)
        run: npm run build
